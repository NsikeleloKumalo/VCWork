@model IEnumerable<cldv6211TheRideYouRent.Models.ReturnCar>

@{
    ViewData["Title"] = "Index";
}

<h1>Return Car</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ReturnId )
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InspectorNoNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RentalNoNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ReturnDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ElapsedDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fine)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ReturnId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.InspectorNoNavigation.InspectorName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.RentalNoNavigation.RentalNo)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ReturnDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ElapsedDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Fine)
                </td>
               @* <td>
                    <a asp-action="Edit" asp-route-id="@item.ReturnId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.ReturnId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.ReturnId">Delete</a>
                </td>*@
            </tr>
        }
    </tbody>
</table>

