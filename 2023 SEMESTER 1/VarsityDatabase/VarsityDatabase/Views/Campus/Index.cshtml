@model IEnumerable<VarsityDatabase.Models.CampusModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Campus_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Campus_Address)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Campus_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Campus_Address)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Campus_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Campus_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Campus_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
