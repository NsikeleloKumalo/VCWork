@model IEnumerable<TheRideYouRentFinalTest.Models.Inspector>

@{
    ViewBag.Title = "Index";
}

<h2>Inspector</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.InspectorName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.InspectorEmail)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.InspectorMobile)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.InspectorName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.InspectorEmail)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.InspectorMobile)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Inspector_no }) |
            @Html.ActionLink("Details", "Details", new { id=item.Inspector_no }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Inspector_no })
        </td>
    </tr>
}

</table>
