@model IEnumerable<VarsityDatabase.Models.LecturerModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Lecturer_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Lecturer_Surname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Lecturer_Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Lecturer_Module)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Lecturer_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Lecturer_Surname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Lecturer_Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Lecturer_Module)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Lecturer_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Lecturer_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Lecturer_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
