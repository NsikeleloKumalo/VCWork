@model IEnumerable<VarsityDatabase.Models.StudentModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Student_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student_Surname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student_Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student_Year)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student_ZipCode)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Student_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Student_Surname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Student_Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Student_Year)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Student_ZipCode)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Student_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Student_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Student_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
